spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/appcenterstudy
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update    #entity로 db테이블 생성해쥬눈 것
    properties:
      hibernate:
        #        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace